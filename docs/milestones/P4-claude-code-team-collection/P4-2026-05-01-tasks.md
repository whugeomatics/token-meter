# P4 2026-05-01 Claude Code Team Usage Collection Tasks

## 目标

按“先 contract、再实现、再验收”的流程完成 Claude Code teammate usage collection。

## 任务

### 1. 文档和 contract

- [x] 创建 P4 README。
- [x] 创建 Claude Code usage event contract。
- [x] 创建 Claude Code ingestion source contract。
- [x] 创建 tool usage report extension contract。
- [x] 创建 P4 design。
- [x] 创建 P4 tasks。
- [x] 创建 P4 acceptance。

### 2. Claude Code source adapter

- [x] 在 collector module 增加 Claude Code source adapter。
- [x] 支持读取本地 Claude Code JSONL：`<user.home>/.claude/projects/**/*.jsonl`。
- [x] 支持 local dashboard 启动和 `/api/ingest` 同时采集 Codex 与 Claude Code。
- [x] 支持读取脱敏 OTel fixture。
- [x] 支持读取受限 hook metadata fixture。
- [x] 明确忽略 prompt、response、raw API body、tool input/output、transcript 正文。
- [x] 为缺失字段设置 `unknown`、0 或 `source_quality`。

### 3. Normalization

- [x] 将 Claude Code metadata 转换为 `TeamUsageEvent`。
- [x] 设置 `tool=claude-code`。
- [x] 生成稳定 `event_key`。
- [x] 本地路径只使用 hash。
- [x] token 字段缺失且总量为 0 时不生成 usage event。

### 4. Collector CLI

- [x] 默认 team collector 同时采集 Codex 和 Claude Code。
- [x] 保留 `--collect-claude-code` 作为旧脚本兼容入口。
- [x] 增加 `--claude-source=<local|otel|hook|fixture>`。
- [x] 增加 `--claude-projects-dir=<path>`。
- [x] 增加 `--claude-otel-input=<path-or-endpoint>`。
- [x] 增加 `--claude-hook-input=<path>`。
- [x] 继续复用 `--server-url`、`--device-token`、`--user-id`、`--device-id`、`--days`。
- [x] stdout 只输出机器可读结果，不输出 token 或 raw payload。

### 5. Server ingestion

- [x] 确认 `/api/team/ingest` 接受 `tool=claude-code`。
- [x] 确认幂等键仍为 `team_id + user_id + device_id + event_key`。
- [x] 确认非法或隐私违规字段不会入库。
- [x] 确认 P3 Codex 上报不受影响。

### 6. Team report

- [x] `/api/team/report` 支持 `tool=codex|claude-code` 可选筛选。
- [x] response 新增 `tools` 聚合。
- [x] period comparison 新增 `comparison.tools`。
- [x] 旧 P3 字段保持兼容。

### 7. Dashboard

- [x] Team 页面增加 All Tools、Codex、Claude Code tool filter。
- [x] Team 页面展示 tool ranking。
- [x] Period Comparison 展示 tool delta。
- [x] 页面不展示 raw source、transcript path 明文或禁止字段。

### 8. 验证

- [x] Maven test 通过。
- [x] Maven package 通过。
- [ ] P3 Codex smoke 或等价回归通过。
- [x] P4 Claude Code fixture smoke 通过。
- [x] 隐私 fixture 验证禁止字段未进入 payload、DB、report、stdout、日志。

## 阶段结束

- [ ] 更新 P4 acceptance 结果。
- [ ] 做 P4 review。
- [ ] 归档 P4 AGENTS。
- [ ] 更新根 README 和 AGENTS 中的当前阶段。
