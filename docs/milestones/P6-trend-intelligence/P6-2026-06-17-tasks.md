# P6 Team Trend Intelligence Tasks

## 1. Contract and API Shape

- [ ] Decide whether `trend_insights` lives inside `/api/team/report` or behind a dedicated Team trend endpoint.
- [ ] Add response DTOs for summary deltas, drivers, signals, alerts, and Markdown summary.
- [ ] Preserve existing `/api/team/report` fields and compatibility.

## 2. Backend Trend Aggregation

- [ ] Compute current vs previous summary deltas for total tokens, net usage, calls, cache hit rate, and reasoning ratio.
- [ ] Compute driver deltas by user, device, model, and tool.
- [ ] Sort drivers by absolute token delta.
- [ ] Handle previous-period zero values without misleading percentages.
- [ ] Add deterministic signals for token spike/drop, cache hit drop, reasoning ratio change, duplicate upload increase, rejected upload increase, and stale device.

## 3. Markdown Summary

- [ ] Generate a concise Markdown summary from current filters, current window, summary deltas, top drivers, and signals.
- [ ] Ensure the summary contains only report-safe fields.

## 4. Team Dashboard UI

- [ ] Add `What changed?` to Team overview.
- [ ] Show top user/device/model/tool drivers.
- [ ] Show trend signals near the summary cards.
- [ ] Add copy Markdown summary action.
- [ ] Display current filters and a clear reset action.
- [ ] Improve empty states based on whether other filters have data.

## 5. Verification

- [ ] Add backend tests for delta math and signal detection.
- [ ] Add frontend script tests for Team trend rendering and Markdown summary.
- [ ] Run `node --check token-meter-app/src/main/resources/static/app.js`.
- [ ] Run `cmd /c D:\Softwares\Maven-3.9.9\bin\mvn.cmd test`.
- [ ] Update P6 acceptance with verification results.
