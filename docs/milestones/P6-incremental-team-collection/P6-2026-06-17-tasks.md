# P6 Incremental Team Collection Tasks

## 1. Contract And State Shape

- [ ] Add collector cursor contract for `(last_successful_event_timestamp, last_successful_event_key)`.
- [ ] Add collector state table or migration for upload cursor state.
- [ ] Preserve existing `/api/team/ingest` payload compatibility.
- [ ] Preserve existing server-side `event_key` deduplication.

## 2. Incremental Upload Selection

- [ ] Capture a collector run cutoff time before scanning.
- [ ] Continue scanning Codex and Claude Code metadata to discover local events.
- [ ] When an upload cursor exists, use it as the source discovery lower bound instead of relying only on the default lookback window.
- [ ] Use the configured initial lookback window only for first run bootstrap with no cursor.
- [ ] Insert discovered events into collector local state with `event_key` idempotency.
- [ ] Select only events after the upload cursor and at or before the cutoff time.
- [ ] Sort upload events by `(event_timestamp, event_key)`.
- [ ] Upload an empty payload heartbeat when there are no pending events.

## 3. Cursor Advancement And Failure Compensation

- [ ] Advance cursor only after the full upload succeeds.
- [ ] Advance cursor to the last uploaded event's timestamp and event key.
- [ ] Do not advance cursor on network error, unauthorized token, invalid response, or rejected events.
- [ ] Keep retry behavior simple: failed runs retry from the previous cursor; server deduplication handles already accepted events from partial failures.
- [ ] Include incremental/cursor metadata in collector stdout without exposing sensitive data.

## 4. Upload Health

- [ ] Ensure repeated no-new-event collector runs do not inflate duplicate upload counts.
- [ ] Ensure successful empty uploads still update Upload Health as a device heartbeat.
- [ ] Keep existing accepted/duplicate/rejected fields compatible.

## 5. Verification

- [ ] Add collector tests for first run upload and cursor advancement.
- [ ] Add collector tests for second run with no new events.
- [ ] Add collector tests for new event after cursor.
- [ ] Add collector tests for same-timestamp event key tie-breaker.
- [ ] Add collector tests for downtime longer than the default lookback window.
- [ ] Add collector tests for failed upload not advancing cursor.
- [ ] Add ingestion/upload health tests for empty heartbeat behavior.
- [ ] If Local Claude Code checkpointing is changed, add regression tests proving repeated Local ingestion remains idempotent and report-compatible.
- [ ] Run `cmd /c D:\Softwares\Maven-3.9.9\bin\mvn.cmd test`.
- [ ] Update P6 acceptance with verification results.
